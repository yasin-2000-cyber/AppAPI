apiVersion: v2
name: ingress-nginx
description: Ingress NGINX controller for the cluster
type: application
version: 0.1.0
dependencies:
  - name: ingress-nginx
    version: "4.12.1"
    repository: "https://kubernetes.github.io/ingress-nginx"
